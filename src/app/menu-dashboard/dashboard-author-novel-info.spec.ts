import { DashboardAuthorNovelInfo } from './dashboard-author-novel-info';

describe('DashboardAuthorNovelInfo', () => {
  it('should create an instance', () => {
    expect(new DashboardAuthorNovelInfo()).toBeTruthy();
  });
});
