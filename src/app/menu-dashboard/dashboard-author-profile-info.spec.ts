import { DashboardAuthorProfileInfo } from './dashboard-author-profile-info';

describe('DashboardAuthorProfileInfo', () => {
  it('should create an instance', () => {
    expect(new DashboardAuthorProfileInfo()).toBeTruthy();
  });
});
